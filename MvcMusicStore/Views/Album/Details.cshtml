@model MvcMusicStore.Models.Album

@{
    ViewBag.Title = "Detaylar";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Şarkı Adı</h2>
<dl class="dl-horizontal">
    @foreach (var item in Model.Kayitlar)
    {

        <dt>@Html.DisplayNameFor(modelItem => item.Sarki.sarki_adi)</dt>
        <dd> @Html.DisplayFor(modelItem => item.Sarki.sarki_adi) </dd>


        <dt>@Html.DisplayNameFor(modelItem => item.Sarki.sarki_suresi)</dt>
        <dd> @Html.DisplayFor(modelItem => item.Sarki.sarki_suresi) </dd>

    }

    </dl>
    <div>
        <h4>Album</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.Album_Adi)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Album_Adi)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.cikis_tarihi)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.cikis_tarihi)
            </dd>

        </dl>
    </div>
    <p>
        @Html.ActionLink("Düzenle", "Edit", new { id = Model.AlbumID }) |
        @Html.ActionLink("Album Listesine Geri Dön", "Index")
    </p>
